# Domain Docs

Before exploring the codebase, read relevant root `CONTEXT.md` and ADRs under `docs/adr/`.

This is a single-context repository. If those files do not exist, proceed silently; create them only when an architectural or domain decision needs recording.

Use the project glossary terms from `CONTEXT.md` when it exists, and explicitly flag conflicts with existing ADRs.
